single '/' fait leak le parsing // DONE
echo + redir close pas le fd // DONE
env n'ecris pasd dans un fd, tjr dans stdout // DONE
exit | exit leak // DONE
si unset path, les commandes lea dans le parsing (command not found) // DONE

???? export leak si pipe + grep // sombre

chmod un file puis echo > file , ca exit au lieu de return
expot ne redirige pas
rajouter un message derreur pour exit si argc > 1 ou si c un alphabet + exit code 2 au lieu de 0


export abc=$extension maybe garbage ==> puis un builtin | builtin ===>leaks



flemme -> 
	cat < loup | cat < notes -> ecrire dans stderr
